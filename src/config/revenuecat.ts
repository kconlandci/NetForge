// ============================================================
// NetForge — RevenueCat Configuration
// Replace the placeholder API key with your real one from
// RevenueCat Dashboard > Project > API Keys > Google Play
// ============================================================

/** Google Play public API key from RevenueCat dashboard */
export const REVENUECAT_API_KEY = "REPLACE_WITH_YOUR_REVENUECAT_GOOGLE_API_KEY";

/** RevenueCat entitlement identifier (configured in RevenueCat dashboard) */
export const ENTITLEMENT_ID = "premium";

/** Product identifier for the Founders Pack (must match Google Play Console) */
export const PRODUCT_ID = "founders_pack";
